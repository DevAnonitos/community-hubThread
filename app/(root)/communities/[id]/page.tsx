import React, { Suspense } from 'react'

const Page = () => {
    return (
        <>
            <Suspense>
                Community page id
            </Suspense>
        </>
    );
};

export default Page;
